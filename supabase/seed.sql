-- Seed data for development
-- This file runs after all migrations

-- Insert a test site (will only work if you have a user in auth.users)
-- Uncomment and adjust after creating your first user:

-- insert into public.sites (user_id, name, domain, timezone)
-- values ('YOUR_USER_UUID', 'My Test Site', 'localhost', 'UTC');
